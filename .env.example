OPENROUTER_API_KEY=
PETRI_MODEL=moonshotai/kimi-k2-0905

# Phase 3 — variant hosting + traffic split
# Vercel Blob (Marketplace integration injects this).
BLOB_READ_WRITE_TOKEN=
# Upstash Redis on Vercel Marketplace injects KV_REST_API_*.
# Locally you can use UPSTASH_REDIS_REST_* — run-store reads either pair.
KV_REST_API_URL=
KV_REST_API_TOKEN=
